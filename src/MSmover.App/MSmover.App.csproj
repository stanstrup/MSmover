<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <RootNamespace>MSmover.App</RootNamespace>
    <AssemblyName>MSmover</AssemblyName>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationIcon>msmover.ico</ApplicationIcon>
    <!--
      Deliberately not a real version number. Release builds are stamped from the git tag by
      build\package.ps1; anything built any other way is a development build and should say so
      rather than impersonating a release in the title bar and in bug reports.
    -->
    <Version>0.0.0-dev</Version>
    <Product>MSmover</Product>
    <Description>Watches an LC-MS acquisition folder and moves finished .raw files to a network drive, with verification.</Description>
    <ApplicationHighDpiMode>PerMonitorV2</ApplicationHighDpiMode>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\MSmover.Core\MSmover.Core.csproj" />
  </ItemGroup>

</Project>
